<div class="row">
	<div class="large-12 columns">
		<h3>Продать билет на <%= @concert.band %> <small><%= @concert.date %> в <%= @hall.name %></small></h3>
	 </div>
</div>

<%= form_for :ticket, url: tickets_path do |f| %>

<div class="row">
    <div class="large-12 columns">
        <%= f.label "Сектор" %>
        <%= react_component('TicketForm', {sections: sections_as_json}) %>
    </div>
</div>

    <div class="large-12 columns">
    <%= f.submit "Добавить", :class => 'button' %>
    </div>
</div>

<% end %>